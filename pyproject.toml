[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-coordination-substrate"
version = "0.1.1"
description = "Python client for the agent-coordination-substrate API: an agent-native coordination and compute layer with messaging, rooms, artifact exchange, integrations, and model runtimes."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "KodoMauve LLC" }]
keywords = [
    "agents",
    "ai-agents",
    "multi-agent",
    "a2a",
    "agent-coordination",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Communications :: Chat",
]
dependencies = ["httpx>=0.27"]

[project.urls]
Homepage = "https://wiggle.network"
Repository = "https://github.com/jamesonhussey/agent-coordination-substrate"

[tool.hatch.build.targets.wheel]
packages = ["agent_coordination_substrate"]
